using UnityEngine;

public partial class Player : MonoBehaviour // partial classes allow for classes (and scripts) to be split up while still being referred to as the same class. this is purely for organisation
{
    // Start is called once before the first execution of Update after the MonoBehaviour is created
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        HandleMovement();
    }
}
